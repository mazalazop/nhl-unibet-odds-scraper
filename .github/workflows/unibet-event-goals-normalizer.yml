name: unibet-event-goals-normalizer

on:
  workflow_dispatch:
    inputs:
      acceptance_report_path:
        description: "Chemin acceptance_report.json (ex: unibet_event_goals_batch_runner/20260310_211120/acceptance_report.json)"
        required: true
        type: string

jobs:
  normalize:
    runs-on: self-hosted
    timeout-minutes: 30

    steps

name: test-manuel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, macOS, X64]
    steps:
      - name: Test
        run: echo "workflow visible OK"
